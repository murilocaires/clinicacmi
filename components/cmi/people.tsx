'use client';
import { useRef, useState } from 'react';
import { ChevronLeft, ChevronRight, Quote } from 'lucide-react';
import { professionals, testimonials } from '@/lib/clinic';
import { SectionHeading } from './shared';
export function Professionals() { const ref = useRef<HTMLDivElement>(null); return <section id="profissionais" className="container professionals"><div className="professionals-panel"><div className="section-top"><SectionHeading eyebrow="Profissionais" title="Conheça nossa equipe"/><div className="carousel-controls"><button aria-label="Profissionais anteriores" onClick={() => ref.current?.scrollBy({ left: -250, behavior: 'smooth' })}><ChevronLeft /></button><button aria-label="Próximos profissionais" onClick={() => ref.current?.scrollBy({ left: 250, behavior: 'smooth' })}><ChevronRight /></button></div></div><div className="professional-list" ref={ref}>{professionals.map(person => <article className="professional-card" key={person.name}><img src={`/images/${person.image}.webp`} alt="Retrato ilustrativo de profissional de saúde" width="180" height="184" loading="lazy"/><div><h3>{person.name}</h3><p>{person.specialty}</p></div></article>)}</div><p className="section-note">Apresentação ilustrativa: fotos, nomes e especialidades de exemplo.</p></div></section>; }
export function Testimonials() { const [index, setIndex] = useState(0); return <section className="container testimonials"><div className="patient-mosaic" aria-hidden="true">{[1, 2, 3, 4, 5].map((n) => <img key={n} src={`/images/doctor-${n}.webp`} alt="" loading="lazy"/>)}<span className="petal blue"/><span className="petal teal"/></div><div className="testimonial-content"><SectionHeading eyebrow="Depoimentos" title="O que nossos pacientes dizem"/><div className="quote-card" aria-live="polite"><Quote className="quote-icon" fill="currentColor"/><blockquote>{testimonials[index]}</blockquote><div className="quote-person"><img src={`/images/doctor-${index + 1}.webp`} alt="" loading="lazy"/><span>Paciente de Serrinha/BA<small>Depoimento ilustrativo</small></span></div></div><div className="dots" aria-label="Selecionar depoimento">{testimonials.map((_, i) => <button key={i} aria-label={`Ver depoimento ${i + 1}`} aria-pressed={i === index} className={i === index ? 'active' : ''} onClick={() => setIndex(i)}/>)}</div></div></section>; }
